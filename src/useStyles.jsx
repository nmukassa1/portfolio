function useStyles() {
    const centerBox = {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    }
    
    return { centerBox };
}

export default useStyles;